## Database Setup Guide

Moved here from `setup-database.md` at the project root.

Quick options:
- **Neon** or **Supabase** for free cloud PostgreSQL; copy the connection string into `DATABASE_URL`.
- **Local PostgreSQL**: create an `interviewai` database and set `DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/interviewai`.


